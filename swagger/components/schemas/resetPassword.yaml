type: object
required:
  - password
  - token
properties:
  password:
    type: string
    format: password
    description: New password
  token:
    type: string
    description: Token from the reset email